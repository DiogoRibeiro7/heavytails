[tox]
envlist = py310,py311,py312,lint,type-check

[testenv]
deps = 
    pytest
    pytest-cov
commands = pytest tests/ --cov=scripts

[testenv:lint]
deps = ruff
commands = ruff check .

[testenv:type-check]
deps = 
    mypy
    types-PyYAML
    types-requests
commands = mypy scripts/
